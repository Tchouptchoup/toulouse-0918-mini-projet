// import { } from '../actions/auth';

const initialState = {
  user: {
    id: 1,
    firstname: 'Maitetxu'
  }
};

const reducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default reducer;